#請寫一個函數，接受兩個列表作為參數，並返回一個新的列表，其中包含從這兩個列表交替取得的元素。例如，輸入 [1, 3, 5] 和 [2, 4, 6] 應該返回 [1, 2, 3, 4, 5, 6]。
input_string1 = input("請輸入第一組由空格分隔的數字：")
numbers1 = input_string1.split( )

input_string2 = input("請輸入第二組由空格分隔的數字：")
numbers2 = input_string2.split( )

def combine(s1,s2):
  k = []
  k.extend(s1)
  k.extend(s2)
  print(k)

combine(numbers1,numbers2)
